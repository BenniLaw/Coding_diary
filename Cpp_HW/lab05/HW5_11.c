# include <stdio.h>
int main(){
    // input
    double data[10] = {0};
    int length = sizeof(data) / sizeof(double);
    for(int i = 0; i < length; i++){
        scanf("%lf", &data[i]);
    }

    // sort
}